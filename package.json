{
  "name": "next-secure-check-workspace",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20.9.0"
  },
  "packageManager": "pnpm@10.20.0",
  "scripts": {
    "build": "pnpm -r --if-present build",
    "lint": "pnpm -r --if-present lint",
    "test": "pnpm test:packages && pnpm test:web",
    "test:packages": "vitest run --passWithNoTests",
    "test:web": "pnpm -C apps/web test",
    "typecheck": "pnpm -r --if-present typecheck",
    "release:gate": "node scripts/v06-quality-gate.mjs",
    "release:gate:v05": "node scripts/v05-release-gate.mjs",
    "clean": "pnpm -r --if-present clean"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.9.0",
    "vite": "8.0.16",
    "vitest": "^4.0.0"
  }
}
